
// NOTE: This should be configured via environment variables in a real-world application.
// The backend application is expected to run on localhost:8080.
export const API_BASE_URL = 'http://localhost:8080';
